server:
  port: 8888

spring:
  main:
    banner-mode: "off"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sql7.freemysqlhosting.net:3306/sql7347194
    username: sql7347194
    password: U2DTdbdPNa
  jpa:
    database: mysql
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
      ddl-auto: update
  freemarker:
    template-loader-path: classpath:/templates
    suffix: .html
  devtools:
    restart:
      additional-paths: /resources

logging:
  level:
    org:
      springframework: ERROR